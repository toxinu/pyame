There is a mini how to create [MineCraft](http://www.minecraft.net/) server on __Debian Lenny__.

# Download
In first, _download_ the .jar [here](http://www.minecraft.net/download/minecraft_server.jar).

# Installation

Now, you have to install Sun Jvm.

The MineCraft dev notice that its better to use the Sun ( _proprietary_ ) __Jvm__.

Add "_non-free_" on each lines in your __/etc/apt/sources.list__ like that :

    deb http://ftp.nerim.net/debian/ lenny main non-free
    deb-src http://ftp.nerim.net/debian/ lenny main non-free
    deb http://security.debian.org/ lenny/updates main non-free
    deb-src http://security.debian.org/ lenny/updates main non-free
    deb http://volatile.debian.org/debian-volatile lenny/volatile main non-free
    deb-src http://volatile.debian.org/debian-volatile lenny/volatile main non-free

Update the sources list :

    apt-get update

And install the Sun Jvm :

    apt-get install sun-java6-jre

# Usage

I notice you to create a little bash script like that :

    #!/bin/bash
    java -Xmx1024M -Xms1024M -jar /root/minecraft/minecraft_server.jar nogui >> /root/minecraft/log_serv 2>&1 &                                

And :

    chmod +x your_script.sh
    ./your_script.sh

With this little bash script you launch your MineCraft Server with a __log_serv__ file with a little histo.

To finish you have to open the __25565__ port on your router.
